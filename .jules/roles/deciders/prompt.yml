role: decider
layer: deciders

contracts:
  - .jules/JULES.md
  - .jules/roles/deciders/contracts.yml
  - .jules/roles/deciders/roles/{{role}}/role.yml

instructions:
  - Read every file in contracts in order before doing any work.
  - Follow contracts.yml exactly (constraints, workflow, inputs/outputs).
  - Output issues must conform to the schema in schemas/issue.yml.
