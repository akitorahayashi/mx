# This file is auto-generated. Do not edit manually.

# Asterism Specialists — Single Observer Specialist Lane
#
# Runs a single observer (selected via workflow_dispatch), skips merger,
# dispatches ready requirements directly to implementer, and sends
# remaining requirements through planner before implementer execution.

name: Asterism Specialists

on:
  workflow_dispatch:
    inputs:
      observer:
        description: 'Observer role to run'
        type: choice
        options:

          - modeler

          - factorer

          - topologist

          - gopher

          - pythonista

          - rustacean

          - swifter

          - typescripter

          - commander

          - interactor

          - taxonomist

          - localizer

          - prompter

          - automator

          - instrumenter

          - proofreader

          - annotator

          - codifier

          - librarian

          - tactician

          - auditor

          - tracer

          - simulator

      wait_minutes:
        description: 'Minutes to wait between layers (Asterism async processing time)'
        type: number
        default: 30
      skip_dispatcher:
        description: 'Skip dispatcher and planner (send observer requirements directly to implementer)'
        type: boolean
        default: false
      mock:
        description: 'Run in mock mode (creates branches/PRs without Asterism API)'
        type: boolean
        default: false

permissions: {}

concurrency:
  group: 'astm-worker'
  cancel-in-progress: false

env:
  WAIT_MINUTES: ${{ github.event.inputs.wait_minutes || 30 }}
  MOCK_MODE: ${{ github.event.inputs.mock == 'true' }}
  ASTM_MOCK_TAG: ${{ format('mock_run_{0}', github.run_id) }}
  ASTM_RUN_FLAGS: ${{ github.event.inputs.mock == 'true' && '--mock' || '' }}
  ASTM_TARGET_BRANCH: 'main'
  ASTM_WORKER_BRANCH: 'jules'
  OBSERVER_ROLE: ${{ github.event.inputs.observer }}




jobs:
  # ============================================================
  # Bootstrap runtime repository on worker branch
  # ============================================================

  bootstrap:
    
    if: >-
      (github.event_name == 'workflow_dispatch' || github.event_name == 'workflow_call') &&
      
      true
      
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'main'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Sync worker branch
        run: astm workflow bootstrap worker-branch

      - name: Bootstrap managed files
        run: astm workflow bootstrap managed-files

      - name: Validate repository structure
        run: astm workflow validate

      - name: Create bootstrap sync PR to worker branch
        id: sync_pr
        run: astm workflow create-sync-pr --preset "bootstrap"

      - name: Wait for bootstrap sync PR merge
        id: wait_for_sync_pr
        if: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').applied == true }}
        env:
          GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
          PR_NUMBER: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').pr_number }}
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm workflow gh inspect-pr-merge "$PR_NUMBER"
          max_attempts: '8'
          retry_on: error
          retry_on_exit_codes: '11'
          # Sync PR merge state has an initial GitHub visibility lag after publication. The first retry waits 3s; later retries use 1,2,4,8,16,32s.
          retry_delay_schedule_seconds: '10,1,2,4,8,16,32'


  # ============================================================
  # Run the selected observer and wait
  # ============================================================

  
  
  
  
  run-observer:
    needs: ["bootstrap"]
    
    if: |
      !cancelled() &&


    
      needs.bootstrap.result == 'success'
    

    
    runs-on: ubuntu-latest
    timeout-minutes: 100
    outputs:
      json: ${{ steps.report.outputs.json }}
    env:
      JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}
      - name: Run observer
        id: run
        if: ${{ needs.bootstrap.result == 'success' }}
        continue-on-error: true
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm run --json observers ${{ env.ASTM_RUN_FLAGS }} --role "${OBSERVER_ROLE}" --task emit_requirements
          max_attempts: '8'
          retry_delay_schedule_seconds: '1,2,4,8,16,32,64'
      - name: Report Run observer status
        id: report
        if: ${{ always() }}
        env:
          REQUESTED_FLAG: ${{ true }}
          REQUEST_RESULT: ${{ steps.run.outcome }}
          RUN_JSON: ${{ steps.run.outputs.final_stdout || '{}' }}
        run: |
          set -euo pipefail
          astm workflow resolve run-status \
            --requested "${REQUESTED_FLAG}" \
            --result "${REQUEST_RESULT}" \
            --run-json "${RUN_JSON}"

      - name: Wait ${{ env.WAIT_MINUTES }} minutes for Asterism async processing
        if: ${{ !cancelled() && always() && steps.report.outcome == 'success' }}
        uses: akitorahayashi/wait@v1
        with:
          enabled: ${{ fromJSON(steps.report.outputs.json || '{}').requested == true }}
          minutes: ${{ env.WAIT_MINUTES }}
          seconds: ${{ env.MOCK_MODE == 'true' && '15' || '' }}
          label: 'Asterism async processing for observers'

  # ============================================================
  # Resolve specialists plan
  # ============================================================
  resolve-specialists-plan:
    needs: [run-observer]
    if: |
      needs.run-observer.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      json: ${{ steps.check.outputs.json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Resolve specialists plan
        id: check
        env:
          SKIP_DISPATCHER: ${{ github.event.inputs.skip_dispatcher || 'false' }}
        run: |
          set -euo pipefail
          astm workflow resolve specialists-plan --skip-dispatcher "$SKIP_DISPATCHER"

  # ============================================================
  # Run dispatcher and wait
  # ============================================================

  
  
  
  
  run-dispatcher:
    needs: ["run-observer", "resolve-specialists-plan"]
    
    if: |
      !cancelled() &&


    
      needs.run-observer.result == 'success' &&
    

    
      needs.resolve-specialists-plan.result == 'success'
    

    
    runs-on: ubuntu-latest
    timeout-minutes: 100
    outputs:
      json: ${{ steps.report.outputs.json }}
    env:
      JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}
      - name: Run dispatcher
        id: run
        if: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_dispatcher == true }}
        continue-on-error: true
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm run --json dispatcher ${{ env.ASTM_RUN_FLAGS }}
          max_attempts: '8'
          retry_delay_schedule_seconds: '1,2,4,8,16,32,64'
      - name: Report Run dispatcher status
        id: report
        if: ${{ always() }}
        env:
          REQUESTED_FLAG: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_dispatcher == true }}
          REQUEST_RESULT: ${{ steps.run.outcome }}
          RUN_JSON: ${{ steps.run.outputs.final_stdout || '{}' }}
        run: |
          set -euo pipefail
          astm workflow resolve run-status \
            --requested "${REQUESTED_FLAG}" \
            --result "${REQUEST_RESULT}" \
            --run-json "${RUN_JSON}"

      - name: Wait ${{ env.WAIT_MINUTES }} minutes for Asterism async processing
        if: ${{ !cancelled() && always() && steps.report.outcome == 'success' }}
        uses: akitorahayashi/wait@v1
        with:
          enabled: ${{ fromJSON(steps.report.outputs.json || '{}').requested == true }}
          minutes: ${{ env.WAIT_MINUTES }}
          seconds: ${{ env.MOCK_MODE == 'true' && '15' || '' }}
          label: 'Asterism async processing for dispatcher'

  # ============================================================
  # Publish ready requirements lane to implementer issues
  # ============================================================

  publish-implementer-requirements:
    needs: ["run-dispatcher", "resolve-specialists-plan"]
    if: |
      !cancelled() &&

      needs.run-dispatcher.result == 'success' &&

      needs.resolve-specialists-plan.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Publish implementer issues (requirements)
        id: publish

        if: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_publish_requirements == true }}

        run: |
          set -euo pipefail
          astm workflow publish implementer-issues requirements

      - name: Validate structure
        run: astm workflow validate

      - name: Create sync PR
        id: sync_pr
        run: |
          astm workflow create-sync-pr \
            --preset "publish-implementer-requirements"

      - name: Wait for implementer issue publication sync PR merge
        id: wait_for_sync_pr
        if: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').applied == true }}
        env:
          GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
          PR_NUMBER: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').pr_number }}
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm workflow gh inspect-pr-merge "$PR_NUMBER"
          max_attempts: '8'
          retry_on: error
          retry_on_exit_codes: '11'
          # Sync PR merge state has an initial GitHub visibility lag after publication. The first retry waits 3s; later retries use 1,2,4,8,16,32s.
          retry_delay_schedule_seconds: '10,1,2,4,8,16,32'

      - name: Roll back published issues on sync failure
        if: ${{ always() && steps.publish.outcome == 'success' && fromJSON(steps.publish.outputs.json || '{}').published_count > 0 && (steps.sync_pr.outcome == 'failure' || (fromJSON(steps.sync_pr.outputs.json || '{}').applied == true && steps.wait_for_sync_pr.outcome == 'failure')) }}
        env:
          GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
          PUBLISHED_JSON: ${{ steps.publish.outputs.json || '{}' }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "Rolling back published implementer issues because worker-branch sync was not completed."
          jq -r '.published[]?.issue_number' <<<"${PUBLISHED_JSON}" | while read -r issue_number; do
            if [ -n "${issue_number}" ]; then
              gh issue close "${issue_number}" --repo "${REPO}"
            fi
          done
  # ============================================================
  # Resolve planner matrix
  # ============================================================

  resolve-planner-matrix:
    needs: ["run-dispatcher"]
    if: |
      !cancelled() &&

      needs.run-dispatcher.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    outputs:
      json: ${{ steps.resolve.outputs.json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Resolve planner targets
        id: resolve

        run: |
          set -euo pipefail
          static_target="${STATIC_TARGET:-}"
          if [ -n "${static_target}" ]; then
            json=$(printf '{"layer":"%s","count":1,"skipped_count":0,"matrix":[{"target":"%s"}]}' "planner" "${static_target}")
            {
              echo "json=${json}"
              echo "count=1"
              echo "skipped_count=0"
              echo "layer=planner"
            } >> "${GITHUB_OUTPUT}"
          elif [ "${MOCK_MODE}" = "true" ]; then
            astm workflow resolve matrix --layer planner --mock
          else
            astm workflow resolve matrix --layer planner
          fi
  # ============================================================
  # Run planner matrix
  # ============================================================

  
  
  
  
  
  request-planner-run:
    needs: ["run-dispatcher", "resolve-planner-matrix", "resolve-specialists-plan"]
    
    if: |
      !cancelled() &&



    
      needs.run-dispatcher.result == 'success' &&
    

    
      needs.resolve-planner-matrix.result == 'success' &&
    

    
      needs.resolve-specialists-plan.result == 'success' &&
    

      fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_planner == true &&
      fromJSON(needs.resolve-planner-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').count > 0
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Asterism API session creation requests must run strictly one-by-one.
    # Keep max-parallel fixed to 1; worker limits are enforced by target truncation at matrix-resolution time.
    # The matrix is kept for visibility of resolved target count in the GitHub Actions GUI.
    strategy:
      matrix:
        include: ${{ fromJSON(needs.resolve-planner-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').matrix }}
      max-parallel: 1
      fail-fast: false
    outputs:
      json: ${{ steps.run.outputs.final_stdout }}
    env:
      JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Run planner (${{ matrix.target }})
        id: run
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm run --json planner --requirement "${{ matrix.target }}" ${{ env.ASTM_RUN_FLAGS }}
          max_attempts: '8'
          retry_delay_schedule_seconds: '1,2,4,8,16,32,64'

  report-planner-status:
    needs: ["run-dispatcher", "resolve-planner-matrix", "resolve-specialists-plan", "request-planner-run"]
    if: |
      !cancelled() &&
      always() &&

      needs.resolve-specialists-plan.result == 'success' &&
      needs.resolve-planner-matrix.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 70
    outputs:
      json: ${{ steps.report.outputs.json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Report Run planner status
        id: report
        env:
          REQUESTED_FLAG: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_planner == true }}
          TARGET_COUNT: ${{ fromJSON(needs.resolve-planner-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').count }}
          REQUEST_RESULT: ${{ needs.request-planner-run.result }}
        run: |
          set -euo pipefail
          astm workflow resolve matrix-run-status \
            --requested "${REQUESTED_FLAG}" \
            --result "${REQUEST_RESULT}" \
            --target-count "${TARGET_COUNT}"

      - name: Wait ${{ env.WAIT_MINUTES }} minutes for Asterism async processing
        if: ${{ !cancelled() && always() && steps.report.outcome == 'success' }}
        uses: akitorahayashi/wait@v1
        with:
          enabled: ${{ fromJSON(steps.report.outputs.json || '{}').requested == true }}
          minutes: ${{ env.WAIT_MINUTES }}
          seconds: ${{ env.MOCK_MODE == 'true' && '15' || '' }}
          label: 'Asterism async processing for planner'

  # ============================================================
  # Publish plans lane to implementer issues
  # ============================================================

  publish-implementer-plans:
    needs: ["report-planner-status", "resolve-specialists-plan"]
    if: |
      !cancelled() &&

      needs.report-planner-status.result == 'success' &&

      needs.resolve-specialists-plan.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Publish implementer issues (plans)
        id: publish

        if: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_publish_plans == true }}

        run: |
          set -euo pipefail
          astm workflow publish implementer-issues plans

      - name: Validate structure
        run: astm workflow validate

      - name: Create sync PR
        id: sync_pr
        run: |
          astm workflow create-sync-pr \
            --preset "publish-implementer-plans"

      - name: Wait for implementer issue publication sync PR merge
        id: wait_for_sync_pr
        if: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').applied == true }}
        env:
          GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
          PR_NUMBER: ${{ fromJSON(steps.sync_pr.outputs.json || '{}').pr_number }}
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm workflow gh inspect-pr-merge "$PR_NUMBER"
          max_attempts: '8'
          retry_on: error
          retry_on_exit_codes: '11'
          # Sync PR merge state has an initial GitHub visibility lag after publication. The first retry waits 3s; later retries use 1,2,4,8,16,32s.
          retry_delay_schedule_seconds: '10,1,2,4,8,16,32'

      - name: Roll back published issues on sync failure
        if: ${{ always() && steps.publish.outcome == 'success' && fromJSON(steps.publish.outputs.json || '{}').published_count > 0 && (steps.sync_pr.outcome == 'failure' || (fromJSON(steps.sync_pr.outputs.json || '{}').applied == true && steps.wait_for_sync_pr.outcome == 'failure')) }}
        env:
          GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
          PUBLISHED_JSON: ${{ steps.publish.outputs.json || '{}' }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "Rolling back published implementer issues because worker-branch sync was not completed."
          jq -r '.published[]?.issue_number' <<<"${PUBLISHED_JSON}" | while read -r issue_number; do
            if [ -n "${issue_number}" ]; then
              gh issue close "${issue_number}" --repo "${REPO}"
            fi
          done
  # ============================================================
  # Resolve implementer matrix
  # ============================================================

  resolve-implementer-matrix:
    needs: ["publish-implementer-requirements", "publish-implementer-plans"]
    if: |
      !cancelled() &&

      needs.publish-implementer-requirements.result == 'success' &&

      needs.publish-implementer-plans.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    outputs:
      json: ${{ steps.resolve.outputs.json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Resolve implementer targets
        id: resolve

        run: |
          set -euo pipefail
          static_target="${STATIC_TARGET:-}"
          if [ -n "${static_target}" ]; then
            json=$(printf '{"layer":"%s","count":1,"skipped_count":0,"matrix":[{"target":"%s"}]}' "implementer" "${static_target}")
            {
              echo "json=${json}"
              echo "count=1"
              echo "skipped_count=0"
              echo "layer=implementer"
            } >> "${GITHUB_OUTPUT}"
          elif [ "${MOCK_MODE}" = "true" ]; then
            astm workflow resolve matrix --layer implementer --mock
          else
            astm workflow resolve matrix --layer implementer
          fi
  # ============================================================
  # Run implementer matrix
  # ============================================================

  
  
  
  
  
  request-implementer-run:
    needs: ["resolve-implementer-matrix", "resolve-specialists-plan"]
    
    if: |
      !cancelled() &&



    
      needs.resolve-implementer-matrix.result == 'success' &&
    

    
      needs.resolve-specialists-plan.result == 'success' &&
    

      fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_implementer == true &&
      fromJSON(needs.resolve-implementer-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').count > 0
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Asterism API session creation requests must run strictly one-by-one.
    # Keep max-parallel fixed to 1; worker limits are enforced by target truncation at matrix-resolution time.
    # The matrix is kept for visibility of resolved target count in the GitHub Actions GUI.
    strategy:
      matrix:
        include: ${{ fromJSON(needs.resolve-implementer-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').matrix }}
      max-parallel: 1
      fail-fast: false
    outputs:
      json: ${{ steps.run.outputs.final_stdout }}
    env:
      JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'main'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Run implementer (${{ matrix.target }})
        id: run
        uses: akitorahayashi/retry@v1
        with:
          command: >-
            astm run --json implementer --issue "${{ matrix.target }}" ${{ env.ASTM_RUN_FLAGS }}
          max_attempts: '8'
          retry_delay_schedule_seconds: '1,2,4,8,16,32,64'

  report-implementer-status:
    needs: ["resolve-implementer-matrix", "resolve-specialists-plan", "request-implementer-run"]
    if: |
      !cancelled() &&
      always() &&

      needs.resolve-specialists-plan.result == 'success' &&
      needs.resolve-implementer-matrix.result == 'success'


    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      json: ${{ steps.report.outputs.json }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'main'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Report Run implementer status
        id: report
        env:
          REQUESTED_FLAG: ${{ fromJSON(needs.resolve-specialists-plan.outputs.json || '{}').run_implementer == true }}
          TARGET_COUNT: ${{ fromJSON(needs.resolve-implementer-matrix.outputs.json || '{"count":0,"skipped_count":0,"matrix":[]}').count }}
          REQUEST_RESULT: ${{ needs.request-implementer-run.result }}
        run: |
          set -euo pipefail
          astm workflow resolve matrix-run-status \
            --requested "${REQUESTED_FLAG}" \
            --result "${REQUEST_RESULT}" \
            --target-count "${TARGET_COUNT}"

  # ============================================================
  # Upload and cleanup summary artifacts
  # ============================================================


  cleanup-summaries:
    needs: ["run-observer", "run-dispatcher", "publish-implementer-requirements", "publish-implementer-plans", "report-implementer-status"]
    if: |
      !cancelled() &&
      always() &&

      (needs.run-observer.result == 'success' || needs.run-observer.result == 'skipped') &&

      (needs.run-dispatcher.result == 'success' || needs.run-dispatcher.result == 'skipped') &&

      (needs.publish-implementer-requirements.result == 'success' || needs.publish-implementer-requirements.result == 'skipped') &&

      (needs.publish-implementer-plans.result == 'success' || needs.publish-implementer-plans.result == 'skipped') &&

      (needs.report-implementer-status.result == 'success' || needs.report-implementer-status.result == 'skipped')

    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Detect summary artifacts
        id: summaries
        shell: bash
        run: |
          if [ -d .astm/exchange/summaries ]; then
            count="$(find .astm/exchange/summaries -maxdepth 1 -type f -name '*.md' | wc -l | tr -d ' ')"
          else
            count="0"
          fi
          if [ "${count}" = "0" ]; then
            echo "has_files=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "has_files=true" >> "$GITHUB_OUTPUT"

      - name: Upload summary artifacts
        if: ${{ steps.summaries.outputs.has_files == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ format('astm-summary-artifacts-{0}-{1}', github.run_id, github.run_attempt) }}
          path: .astm/exchange/summaries/*.md
          retention-days: 3
          if-no-files-found: error

      - name: Install astm
        if: ${{ steps.summaries.outputs.has_files == 'true' }}
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Remove uploaded summary artifacts
        if: ${{ steps.summaries.outputs.has_files == 'true' }}
        run: |
          find .astm/exchange/summaries -maxdepth 1 -type f -name '*.md' -delete

      - name: Validate repository structure
        if: ${{ steps.summaries.outputs.has_files == 'true' }}
        run: astm workflow validate

      - name: Create summary cleanup sync PR
        if: ${{ steps.summaries.outputs.has_files == 'true' }}
        run: |
          astm workflow create-sync-pr \
            --preset "summary-cleanup"

  # ============================================================
  # Cleanup mock artifacts
  # ============================================================


  cleanup-mock-artifacts:
    needs: ["cleanup-summaries"]
    if: |
      !cancelled() &&
      always() &&
      (
        (github.event_name == 'workflow_dispatch' && github.event.inputs.mock == 'true') ||
        (github.event_name == 'workflow_call' && inputs.mock == true)
      )
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.ASTM_BOT_PAT }}
      GITHUB_REPOSITORY: ${{ github.repository }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: 'jules'
          fetch-depth: 0
          token: ${{ secrets.ASTM_BOT_PAT }}


      - name: Configure Git
        run: |
          set -euo pipefail
          git config user.name "aria"
          git config user.email "aria67323@gmail.com"

      - name: Install astm
        uses: asterismhq/setup-astm@v2
        with:
          token: ${{ secrets.ASTM_INSTALL_PAT }}
          version: '27.1.0'
          submodule_token: ${{ secrets.ASTM_SUBMODULE_PAT }}

      - name: Cleanup mock artifacts for this run
        run: |
          astm workflow clean-mock --mock-tag "$ASTM_MOCK_TAG"

      - name: Validate repository structure
        run: astm workflow validate

      - name: Create mock cleanup sync PR
        run: |
          astm workflow create-sync-pr \
            --preset "mock-cleanup-${ASTM_MOCK_TAG}"



